# StarForge 数据录入格式规范

> **用途**：定义"公司沉淀历史商单经验"时需要录入哪些数据、每张表每列怎么写、哪些字段进向量库、哪些必须人工确认。
> **对象**：公司运营 / 商务同学录入；或数据入库管道（意图 ⑧）产出的中间格式。
> **版本**：v1.1（2026-09-22）

---

## 1. 总原则（与铁律一致）

1. **只抽取不生成**：入库的是原始素材里的事实条目，禁止凭空编造。
2. **可溯源**：凡涉及"达人/甲方的脾气、注意事项、经验总结"，必须保留 `source_quote`（原始材料原话），否则无法追溯。
3. **人工确认拦截**：`verified = FALSE` 的条目不参与检索。录入后必须人工确认才生效。
4. **禁止伪精度**：不录入"匹配度/靠谱度"这类无来源评分。`confidence` 只是抽取置信度，不是推荐评分，且仅在 `party_traits` 中出现。

### 1.1 ID 与关联规则

| 表 | 主键格式 | 示例 | 外键 |
|---|---|---|---|
| brands | `B` + 3 位序号 | `B001` | — |
| creators | `C` + 3 位序号 | `C001` | — |
| deals | `D` + 3 位序号 | `D001` | `brand_id`、`creator_id` |
| deal_reviews | `R` + 3 位序号 | `R001` | `deal_id`（须为 settled） |
| party_traits | `T` + 3 位序号 | `T001` | `party_id`（creator_id / brand_id）、`source_deal_id`（可选） |

- ID 全局唯一、**删除后不复用**。
- 录入顺序：`brands → creators → deals → deal_reviews / party_traits`（外键须先存在）。

### 1.2 数据收集清单（按批次准备）

**批次 A：结构化骨架**（照抄即可，不需理解内容）

| 收集项 | 常见来源 | 落到 |
|---|---|---|
| 昵称 / 平台 / 粉丝数 / 女性占比 | 星图·蝉妈妈·飞瓜导出，或历史记录 | `creators` |
| 垂类 / 地域 / 报价（15s·30s·60s·定制） | 商务报价表 | `creators` |
| 品牌名 / 行业 / 对接人 | 合作记录 | `brands` |

**批次 B：商单记录**（运营本来就有的台账）

| 收集项 | 常见来源 | 落到 |
|---|---|---|
| 商单标题 / 状态 / 成交价 / 排期 | 商单台账、合同 | `deals` |
| 甲方原始需求描述 | 需求文档、沟通记录 | `deals.requirement_text` |
| 谈判过程（怎么谈的、改了几轮） | 聊天记录 | `deals.negotiation_notes` |

**批次 C：私有经验**（最值钱，需人工整理）

| 收集项 | 常见来源 | 落到 |
|---|---|---|
| 达人脾气 / 注意事项 | 聊天记录 + 运营总结 | `party_traits` |
| 商单复盘（"下次怎么做"） | 结案复盘 | `deal_reviews` |

> **顺序建议**：先做批次 A（当天即可产出可用数据），再补 B，最后积累 C。原理见 `资产入库与运营成本设计.md` 的冷启动三阶段。

---

## 2. 五张表录入说明

> 字段标记：`必填` = 缺了该行不成立；`进向量库` = 该字段会被语义检索索引；`SQL过滤` = 该字段参与结构化硬过滤。

### 2.1 brands（甲方 / 品牌方）

| 字段 | 类型 | 必填 | 用途 | 说明 |
|---|---|---|---|---|
| brand_id | VARCHAR(16) | ✅ | 主键 | 如 `B001`，全局唯一 |
| brand_name | VARCHAR(128) | ✅ | 展示 | 品牌名 |
| industry | VARCHAR(64) | ✅ | SQL过滤 | 美妆/母婴/食品/3C数码/服饰/宠物/家居 |
| company_size | VARCHAR(32) | — | SQL过滤 | 初创/中型/大型 |
| contact_person | VARCHAR(64) | — | 联系 | 对接人姓名 |
| contact_wechat | VARCHAR(64) | — | 联系 | 微信 |
| cooperation_notes | TEXT | — | **进向量库** | 合作偏好、注意事项、预算风格等自由文本 |

**cooperation_notes 怎么写**：写成完整句子，包含「预算倾向 / 目标人群 / 决策速度 / 内容偏好 / 禁忌」。示例见种子数据 `B001`。

---

### 2.2 creators（达人档案，核心表）

分三类字段：**量化过滤（SQL）**、**文本描述（向量）**、**可信度状态**。

#### 量化过滤字段

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| creator_id | VARCHAR(16) | ✅ | 主键，如 `C001` |
| nickname | VARCHAR(128) | ✅ | 达人昵称 |
| platform | VARCHAR(32) | ✅ | 抖音/小红书/B站 |
| profile_url | VARCHAR(256) | — | 达人主页链接 |
| followers | INTEGER | ✅ | 粉丝数（整数） |
| female_ratio | NUMERIC(4,3) | ✅ | 女性粉丝占比 0.000~1.000 |
| age_18_24_ratio | NUMERIC(4,3) | — | 18-24 岁占比 |
| age_25_34_ratio | NUMERIC(4,3) | — | 25-34 岁占比 |
| avg_views | INTEGER | — | 平均播放量 |
| engagement_rate | NUMERIC(5,4) | — | 互动率（如 0.062） |
| quote_embed_15s | INTEGER | — | 植入 15s 报价（元） |
| quote_embed_30s | INTEGER | — | 植入 30s 报价（元） |
| quote_embed_60s | INTEGER | — | 植入 60s 报价（元） |
| quote_custom | INTEGER | — | 定制整条报价（元） |
| category | VARCHAR(64) | ✅ | 垂类 |
| sub_categories | TEXT[] | — | 细分标签数组 |
| region | VARCHAR(64) | — | 地域 |
| coop_models | TEXT[] | — | `placement`/`custom` 数组，默认二者都支持 |

#### 文本描述字段（全部进向量库）

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| profile_text | TEXT | ✅ | 达人画像描述（人设、粉丝结构、风格） |
| content_style | TEXT | — | 内容风格一句话概括 |
| past_brands | TEXT | — | 历史合作品牌 |
| cooperation_history | TEXT | — | **【核心私有资产】** 历史合作情况总结 |

#### 可信度与状态

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| data_source | VARCHAR(32) | — | manual / 采集 |
| verified | BOOLEAN | — | 默认 FALSE，人工确认后 TRUE |
| status | VARCHAR(16) | — | active / 停用 |

**报价约定（双模式，可同时支持）**：
- 植入（placement）：按 `quote_embed_15s/30s/60s` 固定档位，**不是市场浮动价**。
- 定制（custom）：`quote_custom` 整条报价。
- 不涉及直播，无直播字段。

---

### 2.3 deals（商单记录）

| 字段 | 类型 | 必填 | 用途 | 说明 |
|---|---|---|---|---|
| deal_id | VARCHAR(16) | ✅ | 主键 | 如 `D001` |
| brand_id | VARCHAR(16) | ✅ | 外键 | 关联 brands |
| creator_id | VARCHAR(16) | ✅ | 外键 | 关联 creators |
| title | VARCHAR(256) | ✅ | 展示 | 商单标题 |
| category | VARCHAR(64) | — | SQL过滤 | 垂类 |
| coop_mode | VARCHAR(16) | ✅ | SQL过滤 | `placement`/`custom` |
| embed_duration_sec | INTEGER | — | SQL过滤 | 植入时长（仅 placement） |
| agreed_price | INTEGER | — | SQL过滤 | 成交价 |
| quoted_price | INTEGER | — | SQL过滤 | 报价 |
| revision_count | INTEGER | — | 商务条件 | 约定修改次数 |
| revision_used | INTEGER | — | 商务条件 | 实际用了几次 |
| deliverable | TEXT | — | 商务条件 | 交付内容 |
| exclusive_days | INTEGER | — | 商务条件 | 独家期（天） |
| authorization_scope | VARCHAR(128) | — | 商务条件 | 素材授权范围 |
| signed_date | DATE | — | 排期 | 签约日期 |
| publish_deadline | DATE | — | 排期 | 发布截止 |
| published_date | DATE | — | 排期 | 实际发布 |
| status | VARCHAR(32) | ✅ | SQL过滤 | negotiating/signed/producing/published/settled/cancelled |
| settlement_amount | INTEGER | — | 财务 | 结算金额 |
| requirement_text | TEXT | — | **进向量库** | 甲方原始需求描述 |
| negotiation_notes | TEXT | — | **进向量库** | 谈判过程记录【核心私有资产】 |
| result_views | INTEGER | — | 效果 | 结果播放量 |
| result_engagement | INTEGER | — | 效果 | 结果互动量 |

**状态机约定**：未完成的单（negotiating/signed/producing）**不应有复盘**；settled 后才能写 `deal_reviews`。

---

### 2.4 deal_reviews（商单复盘，核心私有资产）

| 字段 | 类型 | 必填 | 用途 | 说明 |
|---|---|---|---|---|
| review_id | VARCHAR(16) | ✅ | 主键 | 如 `R001` |
| deal_id | VARCHAR(16) | ✅ | 外键 | 关联 deals（须是 settled） |
| reviewer_role | VARCHAR(32) | ✅ | 维度 | `agency`（中介）/`brand`（甲方）/`creator`（达人） |
| rating | INTEGER | — | 参考 | 1~5 |
| content | TEXT | ✅ | **进向量库** | 复盘正文：经验教训、可复用的结论 |

**content 怎么写**：不要只复述过程，要落到"下次怎么做"的可复用经验。同一商单可有多条（agency/brand/creator 三视角），种子数据 D003、D006、D012、D013 有示例。

---

### 2.5 party_traits（达人/甲方脾气与注意事项，最核心私有资产）

| 字段 | 类型 | 必填 | 用途 | 说明 |
|---|---|---|---|---|
| trait_id | VARCHAR(16) | ✅ | 主键 | 如 `T001` |
| party_type | VARCHAR(16) | ✅ | 归属 | `creator`/`brand` |
| party_id | VARCHAR(16) | ✅ | 归属 | 对应 creator_id 或 brand_id |
| trait_category | VARCHAR(32) | ✅ | 分类 | 沟通偏好/改稿态度/排期习惯/付款要求/内容尺度/其他 |
| trait_content | TEXT | ✅ | 内容 | 抽取出的具体条目（一句话） |
| severity | VARCHAR(16) | — | 分级 | `info`/`warning`/`critical` |
| source_deal_id | VARCHAR(16) | — | 溯源 | 来源商单（可为空，见下） |
| source_quote | TEXT | — | **防幻觉关键** | 原始材料原话引用 |
| source_channel | VARCHAR(32) | — | 溯源 | `manual_note`/`screenshot`/`import`/`chat_export` |
| source_ref | TEXT | — | 溯源 | 截图文件路径 / 消息 ID |
| confidence | NUMERIC(3,2) | — | 抽取置信度 | 0~1，**不是推荐评分** |
| verified | BOOLEAN | — | **人工确认拦截** | 仅 TRUE 参与检索 |

**四条硬约束**：

1. `source_quote` 必须保留原文引用（聊天记录 / 复盘笔记原话），禁止用总结代替。
2. `verified = FALSE` 不参与检索，录入后等待人工确认。
3. `trait_content` 是抽取的**事实条目**，不是二次生成的经验总结。
4. `severity = critical` 表示"踩了会出事故"级别的注意事项，应优先展示。

**source_deal_id 可为空的情况**：条目来自非单笔商单的长期观察（如种子数据 `T011`、`T021`），此时 `source_quote` 仍建议保留出处说明。

---

## 3. 录入流程与人工确认

```
原始素材（聊天记录 / 复盘笔记 / 报价单）
        │
        ▼
LLM 抽取结构化条目（只抽取，不生成）
        │
        ▼
写入表（verified = FALSE）
        │
        ▼
人工确认（核对 source_quote 是否忠实原文）
        │
        ├─ 通过 → verified = TRUE，进入检索
        └─ 不通过 → 修正 / 删除
```

**注意**：
- 只有 `verified = TRUE` 的 `party_traits` 与 `creators` 会进入向量检索。
- 录入是"对外的动作"，按全局拦截机制需人工确认后再写入。

---

## 4. 枚举值约定

| 字段 | 允许值 |
|---|---|
| platform | 抖音 / 小红书 / B站 |
| category | 美妆 / 母婴 / 食品 / 3C数码 / 服饰 / 宠物 / 家居 / 汽车 / 游戏 / 教育 / 旅游 / 健身 / 健康 / 财经 / 本地生活 / 图书文化 / 二次元 / 情感剧情 |
| coop_mode | placement / custom |
| coop_models | placement、custom（数组） |
| deals.status | negotiating / signed / producing / published / settled / cancelled |
| reviewer_role | agency / brand / creator |
| trait_category | 沟通偏好 / 改稿态度 / 排期习惯 / 付款要求 / 内容尺度 / 其他 |
| severity | info / warning / critical |
| party_type | creator / brand |
| company_size | 初创 / 中型 / 大型 |

---

## 5. 与种子数据的对应

种子数据文件 `data/seed_data.json` 顶层键与表一一对应：

```json
{
  "_meta": { ... },
  "brands":       [ ... ],   // → brands
  "creators":     [ ... ],   // → creators
  "deals":        [ ... ],   // → deals
  "deal_reviews": [ ... ],   // → deal_reviews
  "party_traits": [ ... ]    // → party_traits
}
```

> 字段名与表列名**完全一致**（snake_case），`init_db.py` 按字段名直接映射。新增/修改字段时，需同步改三处：`schema.sql`（建表）、`init_db.py`（导入语句）、本文档。

---

## 6. 录入模板（可直接复制填写）

### 6.1 五张表 JSON 骨架

```json
{
  "brands": [
    {
      "brand_id": "B001", "brand_name": "", "industry": "",
      "company_size": "", "contact_person": "", "contact_wechat": "",
      "cooperation_notes": ""
    }
  ],
  "creators": [
    {
      "creator_id": "C001", "nickname": "", "platform": "", "profile_url": "",
      "followers": 0,
      "female_ratio": 0.000, "age_18_24_ratio": null, "age_25_34_ratio": null,
      "avg_views": null, "engagement_rate": null,
      "quote_embed_15s": null, "quote_embed_30s": null, "quote_embed_60s": null,
      "quote_custom": null, "category": "", "sub_categories": [],
      "region": "", "coop_models": ["placement", "custom"],
      "profile_text": "", "content_style": "", "past_brands": "",
      "cooperation_history": "", "data_source": "manual",
      "verified": false, "status": "active"
    }
  ],
  "deals": [
    {
      "deal_id": "D001", "brand_id": "B001", "creator_id": "C001", "title": "",
      "category": "", "coop_mode": "placement", "embed_duration_sec": null,
      "agreed_price": null, "quoted_price": null,
      "revision_count": null, "revision_used": null, "deliverable": "",
      "exclusive_days": null, "authorization_scope": "",
      "signed_date": null, "publish_deadline": null, "published_date": null,
      "status": "negotiating", "settlement_amount": null,
      "requirement_text": "", "negotiation_notes": "",
      "result_views": null, "result_engagement": null
    }
  ],
  "deal_reviews": [
    {
      "review_id": "R001", "deal_id": "D001", "reviewer_role": "agency",
      "rating": null, "content": ""
    }
  ],
  "party_traits": [
    {
      "trait_id": "T001", "party_type": "creator", "party_id": "C001",
      "trait_category": "", "trait_content": "", "severity": "info",
      "source_deal_id": null, "source_quote": "",
      "source_channel": "manual_note", "source_ref": "",
      "confidence": null, "verified": false
    }
  ]
}
```

### 6.2 单条示例：新增一条达人注意事项

```json
{
  "trait_id": "T031",
  "party_type": "creator",
  "party_id": "C001",
  "trait_category": "付款要求",
  "trait_content": "结算需在发布后 3 个工作日内完成",
  "severity": "warning",
  "source_deal_id": "D001",
  "source_quote": "聊天记录原话：「我们这边流程是发完三天内结，晚了就不好约下次了」",
  "source_channel": "manual_note",
  "source_ref": "screenshots/2026-09-21/D001-结算沟通.png",
  "confidence": 0.9,
  "verified": false
}
```

> `verified: false` → 待人工确认，确认后置 `true` 才参与检索。

---

## 7. 导入方式（日常增量 vs 全量重建）

### 7.1 日常录入：增量导入（推荐，不动已有数据）

新增 / 修改记录时用 `import_data.py`，它只做 upsert，**不会清空任何数据**：

```powershell
cd C:\Users\Administrator\WorkBuddy\2026-09-21-10-45-59\starforge

# 导入整个数据文件的 5 张表
.venv\Scripts\python.exe src/db/import_data.py

# 只导入某一张表（如刚补了几条达人注意事项）
.venv\Scripts\python.exe src/db/import_data.py --table party_traits

# 从另一个文件导入（如新收回来的一批数据）
.venv\Scripts\python.exe src/db/import_data.py --file data/batch_2026_10.json
```

输出示例：

```
  creators       新增   12 条　更新    3 条
合计：新增 12 条，更新 3 条（已有数据未被删除）
```

**规则**：主键（`C001` 等）已存在 → 更新该行；不存在 → 新增该行；其余数据一律不动。

> 增量导入**不会删除**记录。要删记录需单独执行 `DELETE`，或改用 7.3 的全量重建。

### 7.2 同步向量库（改了文本字段时必做）

```powershell
.venv\Scripts\python.exe src/retrieval/vectorize.py
```

`vectorize.py` 按数据库内容做 `upsert`，**重复跑安全**，不会重复累积。

### 7.3 全量重建（仅用于改表结构 / 彻底重来）

**⚠️ 会清空所有数据**（`schema.sql` 含 `DROP TABLE`），仅在下列情况使用：

- 首次初始化数据库；
- 修改了 `schema.sql`（增删列）；
- 想彻底重来一遍。

```powershell
.venv\Scripts\python.exe src/db/init_db.py
```

### 7.4 校验

```powershell
.venv\Scripts\python.exe eval/verify_data.py    # 行数、边界样本、分布
.venv\Scripts\python.exe eval/verify_vector.py  # 向量检索
```

**校验要点**：
- 各表行数与预期一致（脚本会打印）。
- 边界样本 `C019`（0.690）应被"女性占比≥0.70"排除、`C003`（0.710）应被纳入——不符则说明过滤字段填错。
- 改动文本字段后**必须重跑 `vectorize.py`**，否则向量库与数据库不一致。

> **编码提示**：数据文件按 UTF-8 保存即可，**带 BOM 也能识别**（记事本 / Excel / PowerShell `Out-File` 导出的文件常带 BOM，脚本已兼容）。

---

## 8. 常见录入错误

| 错误 | 后果 | 正确做法 |
|---|---|---|
| `female_ratio` 填成 `78`（百分数） | 过滤条件全部失效 | 填 `0.78`（0~1 小数） |
| 外键指向不存在的 ID | 导入报错 | 先录 `brands` / `creators` |
| `party_traits.verified` 忘置 `true` | 条目不参与检索（静默失效） | 人工确认后置 `true` |
| `deal_reviews` 挂在未结案的商单上 | 逻辑矛盾 | 只有 `settled` 的单才写复盘 |
| `source_quote` 填成自己的总结 | 违反防幻觉铁律 | 必须是原始材料原话 |
| 改了文本字段但没重跑 `vectorize.py` | 向量库与库不一致 | 改完即重跑 |
| `coop_mode` 用了 `livestream` 等值 | 枚举越界 | 只有 `placement` / `custom`（无直播） |
