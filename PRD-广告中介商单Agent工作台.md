# PRD：广告中介商单 Agent 工作台

> **项目代号**：StarForge（星锻）
> **文档类型**：产品需求文档（简历作品版）
> **版本**：v1.1
> **日期**：2026-09-21
> **作者**：个人项目
>
> **v1.1 变更记录**（模型部署方案调整 + 数据模型细化）：
> 1. **放弃模型自部署**，改为「Embedding 本地 Ollama + 主模型调 DashScope API」
>    - 原因：魔搭 Notebook 不提供稳定的免鉴权公网服务（详见第 5.1.5 节踩坑记录）
> 2. **报价模型细化**：植入（按时长固定档位）/ 定制（达人报价）双模式
> 3. **新增 `party_traits` 表**：达人/甲方的脾气与注意事项，公司核心私有资产
> 4. **明确「抽取但不生成」原则**：查询时只列事实条目，不现场生成总结（防幻觉）
> 5. **V1 暂不启用 Rerank**（4GB 显存限制），作为后续对比实验

---

## 0. 阅读指引

本文档面向两类读者，写法上做了兼顾：

| 读者 | 关注章节 | 说明 |
|---|---|---|
| 面试官 / 技术评审 | 第 3、5、6、7 章 | 架构设计、技术选型与取舍论证 |
| 业务方 / 潜在用户 | 第 1、2、4 章 | 价值论证、功能需求 |

**核心原则**：本文档中每一个技术决策都必须能回答"为什么这么做，而不是那么做"。凡是没有替代方案对比的设计，一律视为未完成设计。

---

## 1. 项目概述

### 1.1 一句话定义

> 一个面向 **广告中介公司（MCN / 星图服务商）** 的**私有化 AI 工作台**，把公司自己的历史商单经验沉淀为可检索资产，用多 Agent 串起「刊例生成 → 达人商单匹配 → 建联对齐 → 客户沟通」全流程。

### 1.2 关键定位（重要）

本项目的定位，是在完成真实市场调研后**收窄**得出的，而非最初设想。

**调研发现的三个事实**：

1. **达人的公开数据已经被做透了。** 巨量星图官方提供达人总榜、行业榜、量化筛选；蝉妈妈、飞瓜数据、千瓜数据等第三方工具覆盖抖音/快手/B站/小红书全平台达人画像、粉丝兴趣标签、历史带货表现。这些是**平台方和头部数据公司的主场**。

2. **但"公司自己的经验"没有任何工具沉淀。** 一家中介公司去年谈成的某单：报价怎么定的、达人当时的顾虑是什么、甲方改了几次需求、最后怎么谈拢的——这些**只存在于员工个人微信和脑子里**。

3. **大厂做不了这件事。** 因为每家中介公司的商单数据、话术风格、客户结构都不一样，无法标准化成 SaaS，只能私有化部署。**这是本项目的机会窗口。**

### 1.3 差异化声明

| 别人已经做的（不碰） | 本项目做的（唯一性） |
|---|---|
| 达人公开数据查询、榜单 | ❌ 不碰，纯数据查询已被做死 |
| 达人标签筛选 | ⚠️ 做，但必须附带"可溯源的匹配理由" |
| —— | ✅ **中介公司私有商单资产的沉淀与复用** |
| —— | ✅ **建联后对齐问题的显性化** |
| —— | ✅ **内网私有化部署，数据不出域** |
| —— | ✅ **自然语言驱动的 Agent 化工作台** |

**一句话**：不是"又一个达人数据工具"，而是**广告中介公司的私有商单协作工作台**。

---

## 2. 市场调研与竞品分析

### 2.1 第一层：平台方系统（业务逻辑参照）

| 系统 | 定位 | 关键功能 |
|---|---|---|
| **巨量星图** | 抖音官方达人商单撮合平台 | 达人总榜/行业榜/量化筛选、任务发布、内容审核、结算、MCN管理 |
| **星图服务商体系** | **本项目的目标用户就是此角色** | 达人筛选、**建联**、内容策划、项目进度管理、投放复盘、账期垫款 |
| **小红书花火** | 小红书官方撮合平台 | 同类逻辑，侧重图文种草 |
| **抖音林客** | 服务商订单管理与推广协作 | 服务商-商家合作全流程 |

**关键结论**：星图服务商的官方职责表述为「为客户提供**达人筛选、建联**、内容策划、项目进度管理、投放复盘等服务」。**本项目的功能范围与之高度重合，说明需求真实存在，且已被平台定义为需要"服务商"这个角色来承担的专业工作。**

### 2.2 第二层：商业数据工具（产品设计参照）

| 工具 | 强项 | 对本项目的意义 |
|---|---|---|
| **蝉妈妈 / 蝉圈圈** | 达人画像、粉丝标签、**分行业筛选**、投放报告自动生成、**AI商务智能推荐** | 功能最接近。**其"蝉圈圈"已推出AI商务助手，说明该方向已被市场验证**；也说明纯数据筛选赛道已拥挤 |
| **飞瓜数据** | 多平台覆盖、实时带货数据、跨平台归因 | 达人库字段设计可参考 |
| **千瓜数据** | 小红书种草、笔记榜单 | 花火侧对标参考 |
| **HypeAuditor / Modash / CreatorIQ** | 假粉检测、3.5亿达人库、语义搜索、企业级CRM | 功能天花板参考 |

**关键结论**：**纯"达人筛选+公开数据"这条路已被占满，不能正面竞争。** 本项目必须从"私有经验资产"切入。

### 2.3 第三层：开源工作台（技术底座候选）

| 项目 | 定位 | 许可证 | 本项目用途 |
|---|---|---|---|
| **Dify** | 开源 LLM 应用平台，可视化工作流+知识库+多模型 | Apache 2.0（有附加条款） | **✅ 选定为工作台外壳** |
| **FastGPT** | 专注知识库问答，中文 RAG 强 | Fair Code（需商业授权） | 知识库交互参考 |
| **RAGFlow** | 文档解析最强（表格/图文混排） | Apache 2.0 | 商单资料解析方案参考 |
| **Coze Studio** | 字节开源，可视化 Agent 开发 | 开源 | 备选外壳 |
| **LangGraph** | 多 Agent 编排，Supervisor 模式 | MIT | **✅ 选定为 Agent 编排层** |

**底座选型决策**：
- **Dify 做壳**：可视化工作流、内置知识库管理、多模型接入、自带前后端 UI，**可省去 60% 以上的基础开发工作量**。
- **LangGraph 做 Agent 编排**：Dify 的编排能力偏向"流程图式"，对**多 Agent 协同 + 条件路由 + 状态持久化 + 人工中断**支持不足。LangGraph 的 StateGraph 是这一层的工业标准。
- **两者结合方式**：Dify 负责 UI、会话管理、知识库 CRUD、模型接入；复杂 Agent 逻辑封装为独立服务，通过 HTTP 工具节点接入 Dify。

### 2.4 竞品分析总结论

```
             公开数据  ←─────────────→  私有经验
                │                          │
 平台方  ┌───────┴────────┐                 │
         │ 巨量星图/花火   │                 │
         │ （官方，免费）  │                 │
         └────────────────┘                 │
                │                          │
 数据商  ┌───────┴────────┐        ┌────────┴────────┐
         │ 蝉妈妈/飞瓜/千瓜│        │   本 项 目      │
         │ （SaaS，付费）  │        │ 私有化 / 经验库 │
         └────────────────┘        └─────────────────┘
                ↑                          ↑
          大规模、标准化              小规模、非标准
          大厂的主场                无人占据的窗口
```

---

## 3. 系统架构设计

### 3.1 整体架构

```
┌─────────────────────────────────────────────────────────────────┐
│                    用户层（Web 工作台）                           │
│  ┌──────────┬──────────────────────┬───────────────────────┐    │
│  │ 左侧导航 │      中间对话区       │    右侧结构化面板      │    │
│  │          │                      │                       │    │
│  │ 会话列表 │  Agent 对话输出       │  达人卡 / 商单卡      │    │
│  │ 功能入口 │  意图路由可视化       │  匹配理由 / 待确认项   │    │
│  │ 知识库   │                      │  刊例预览             │    │
│  └──────────┴──────────────────────┴───────────────────────┘    │
└────────────────────────────┬────────────────────────────────────┘
                             │ HTTP / SSE
┌────────────────────────────▼────────────────────────────────────┐
│                  应用层：Dify（工作台外壳）                       │
│  · 会话管理  · 知识库 CRUD  · 模型接入  · 用户与权限              │
└────────────────────────────┬────────────────────────────────────┘
                             │ Tool Node (HTTP)
┌────────────────────────────▼────────────────────────────────────┐
│              编排层：LangGraph（Agent 编排服务）                  │
│                                                                  │
│   ┌──────────────────────────────────────────────────────┐      │
│   │  Router（意图识别，LLM-based）                        │      │
│   └───────┬──────────────────────────────────────────────┘      │
│           │                                                      │
│   ┌───────▼──────────────────────────────────────────────┐      │
│   │  Agent Nodes                                          │      │
│   │  Chat │ RAG │ PublicData │ Copywriting │ RateCard     │      │
│   │  Match│ Align│ Ingest(管道)                          │      │
│   └───────┬──────────────────────────────────────────────┘      │
│           │                                                      │
│   ┌───────▼──────────────────────────────────────────────┐      │
│   │  Human-in-the-loop Gate（全局拦截层）                 │      │
│   │  对外动作前强制中断，等待人工确认                      │      │
│   └──────────────────────────────────────────────────────┘      │
└──────┬──────────────────┬──────────────────┬────────────────────┘
       │                  │                  │
┌──────▼──────┐  ┌────────▼────────┐  ┌─────▼──────────────┐
│ 检索层       │  │ 结构化数据层     │  │ 模型服务层          │
│             │  │                 │  │                    │
│ 向量库      │  │ PostgreSQL      │  │ Embedding（本地）   │
│ (ChromaDB)  │  │ · 达人表        │  │ · Ollama + bge-m3  │
│             │  │ · 商单表        │  │   或 Qwen3-Emb-0.6B│
│ 混合检索    │  │ · party_traits  │  │                    │
│ 向量+SQL过滤│  │   （私有资产）   │  │ 主模型（云端 API）  │
│             │  │                 │  │ · DashScope DeepSeek   │
│ Rerank(V2)  │  │                 │  │                    │
└─────────────┘  └─────────────────┘  └────────────────────┘
```

### 3.2 为什么必须做混合检索（核心设计决策）

**问题**：中介的真实查询不是纯语义查询。

举例："帮我找粉丝 5-30 万、女性占比 70% 以上、报价 1 万以内的美妆达人"

| 查询成分 | 类型 | 纯向量检索能处理吗 |
|---|---|---|
| "美妆达人" | 语义 | ✅ 能 |
| "粉丝 5-30 万" | 数值范围过滤 | ❌ 不能 |
| "女性占比 70%+" | 数值范围过滤 | ❌ 不能 |
| "报价 1 万以内" | 数值范围过滤 | ❌ 不能 |

**结论**：**纯向量检索在此场景必然失败。** 必须采用：

```
用户需求
   │
   ├─→ [LLM 需求解析] → 结构化过滤条件 + 语义查询文本
   │                         │                    │
   │                         ▼                    ▼
   │                   SQL WHERE 过滤       向量相似度检索
   │                   （PostgreSQL）        （向量库）
   │                         │                    │
   │                         └────────┬───────────┘
   │                                  ▼
   │                          候选集合并（取交集/加权）
   │                                  ▼
   └──────────────────────→  [Rerank 重排]
                                      ▼
                              Top-K 结果 + 可溯源理由
```

**这一设计直接决定项目的技术含金量**，也是面试中的重点提问区。

### 3.3 匹配可解释性的实现（必须溯源）

**反例（禁止）**：
> "该达人匹配度 82%"

**问题**：82% 是权重拍脑袋算出来的伪精度，甲方追问"怎么算的"就答不上来。

**正例（要求）**：
> "推荐理由：
> 1. 该达人女性粉丝占比 **73%**（数据源：达人档案表 fan_gender_ratio 字段），甲方目标人群以女性为主 ✅
> 2. 近 3 单同类商单均价 **1.2 万**（数据源：合作记录表 3 条历史记录），甲方预算 1 万，**略超预算 20%，建议议价** ⚠️
> 3. 历史合作甲方中，与本次甲方同属美妆品类 **2 家**（数据源：合作记录表）"

**实现要求**：
- 每一条推荐理由，必须能指向**具体的数据字段或记录 ID**；
- 前端支持点击理由 → 跳转到原始数据；
- 不允许出现无数据来源的评分数字。**

### 3.4 人工确认拦截层设计

**为什么它是架构层而非意图**：

"人工确认"不是用户想做什么，而是**系统在危险动作前必须停下来**。它跨越所有意图存在，若设计为意图会导致路由逻辑反复跳转（执行到一半跳回确认，再跳回去）。

**正确实现（LangGraph）**：

```python
# 节点元数据标注是否需要人工确认
NODE_CONFIG = {
    "send_message":   {"external": True,  "require_confirm": True},
    "generate_quote": {"external": True,  "require_confirm": True},
    "ingest_data":    {"external": False, "require_confirm": True},  # 入库也需确认
    "search_kb":      {"external": False, "require_confirm": False},
}

# 在图中插入拦截节点，使用 checkpointer 实现中断与恢复
graph.add_conditional_edges("router", route_fn)
for node_name, cfg in NODE_CONFIG.items():
    if cfg["require_confirm"]:
        graph.add_node(f"{node_name}_gate", human_confirm_gate)
        graph.add_edge(node_name, f"{node_name}_gate")

app = graph.compile(checkpointer=PostgresSaver(...))  # 支持中断后恢复
```

**必须人工确认的动作清单**：
1. 向客户/达人**发送任何消息**
2. 生成并**对外输出报价单**
3. **写入知识库**（防脏数据污染检索结果）
4. 生成**刊例**（涉及商业报价）

---

## 4. 功能需求清单

### 4.1 意图体系（已确认版本）

**结构：7 个用户意图 + 1 个自动管道 + 1 个全局拦截机制**

| 层 | 编号 | 意图 | 路由去向 | 说明 |
|---|---|---|---|---|
| **交互层** | ① | 联网通用聊天 | 主模型 + 搜索工具 | 兜底，不查知识库 |
| | ② | 公司资产咨询 | 本地 RAG（混合检索+重排） | 查私有知识库 |
| | ③ | 公开数据咨询 | 外部数据源/API | 查达人公开信息 |
| | ④ | 话术构思 | 专用 Prompt Agent | 建联沟通话术 |
| | ⑤ | 刊例生成 | 结构化输出 Agent | 生成标准化刊例 |
| | ⑥ | 达人↔商单匹配 | 混合检索 + 可溯源理由 | **核心功能** |
| | ⑦ | 对齐问题清单 | 信息抽取 + 比对 Agent | **核心功能** |
| **管道层** | ⑧ | 数据入库 | 入口功能 + 后台管道 | **自动触发，带人工确认** |
| **架构层** | ⑨ | 人工确认拦截 | 全局机制（非意图） | 挂在对外动作节点上 |

### 4.2 核心功能详细定义

#### 功能 A：刊例生成（意图 ⑤）

**输入**：达人昵称/主页链接 + 基础信息（可部分缺失）
**输出**：标准化刊例卡

| 字段 | 来源 | 是否必填 |
|---|---|---|
| 达人昵称 / ID | 输入或知识库 | 必填 |
| 平台 | 抖音 / 小红书 / B站 | 必填 |
| 粉丝量级 | 输入或公开数据 | 必填 |
| 粉丝画像（性别/年龄/地域） | 输入或公开数据 | 建议 |
| 内容垂类 | 模型判断 | 必填 |
| 历史合作品牌 | 知识库 | 建议 |
| **报价区间** | 知识库历史成交价 | **必填** |
| 合作形式与交付 | 输入 | 必填 |
| 备注（坑位费/排期/修改次数） | 知识库 | 建议 |

**价值点**：新人不用再从零整理 Excel，输入昵称即出标准刊例。

#### 功能 B：达人↔商单匹配（意图 ⑥）

**内部流程**：

```
甲方需求（可能是一段微信大白话）
   │
   ▼
[步骤 1] 需求解析 → 结构化条件
   {平台, 品类, 粉丝量区间, 粉丝性别, 粉丝年龄, 预算上限, 时间要求, 交付形式}
   │
   ▼
[步骤 2] 混合检索 → 候选集
   SQL 过滤（硬条件）+ 向量检索（语义）→ 交集
   │
   ▼
[步骤 3] Rerank 重排 → Top-K
   │
   ▼
[步骤 4] 理由生成 → 每条理由绑定数据来源
   │
   ▼
[步骤 5] 人工确认 → 输出推介单
```

**注意**：原设计中的"甲方需求解析"是独立意图，**现已降级为匹配 Agent 的内部步骤**。理由：用户不会专门说"帮我解析需求"，他是说"帮我找达人"——做成独立意图会让用户多点一步。

#### 功能 C：对齐问题清单（意图 ⑦）

**定位（重要）**：**"把隐性分歧显性化"，而不是"自动解决分歧"。**

**输入**：甲方需求 + 达人承接条件 + 历史沟通记录
**输出**：待确认问题清单 + 差距表

| 项 | 甲方要求 | 达人条件 | 差距 | 待确认 |
|---|---|---|---|---|
| 修改次数 | 3 次 | 含 1 次 | 差 2 次 | 是否加价？ |
| 发布时间 | 下周三 | 下周五可发 | 晚 2 天 | 能否提前？ |
| 素材归属 | 要求二次授权 | 通常不含 | 涉及授权费 | 是否购买？ |

**实现**：LLM 抽取两侧条件 → 逐字段比对 → 差异标注 → 生成话术草稿。

**宣传口径警告**：不要宣传"自动对齐"。正确表述是"**3 秒告诉你这单还有哪些坑没确认**"。

#### 功能 D：数据入库管道（意图 ⑧）

**触发方式**：
1. **主动**：用户上传 Excel / 粘贴聊天记录
2. **自动**：系统识别内容类型（新达人 / 新商单 / 新反馈）并提示入库

**处理流程**：
```
原始输入（Excel / 聊天记录 / 文本）
   │
   ▼
[LLM 结构化抽取] → 抽取字段 + 置信度
   │
   ▼
[低置信度标记] → 置信度 < 阈值 的字段高亮
   │
   ▼
[人工确认界面] ← 【必须环节，不可跳过】
   │
   ▼
[双写] → 结构化字段写 PostgreSQL + 文本块向量化写向量库
```

**关键约束**：
- **入库必须经人工确认**。理由：从口语化聊天记录抽取"坑位费 1.2 万、含 1 次修改、下周三发"必然有错。错误数据进入知识库后，检索出来的就是错的，且难以排查。
- **入库是项目成败的关键环节**。知识库空着，前面所有 Agent 都是演空城计。

#### 功能 E：私有经验资产沉淀（party_traits）★ 核心差异化

**背景**：达人的"小毛病"——比如"必须提前 2 周给 brief""不能接受改稿超过 2 次""必须发原片"——**任何公开数据工具都查不到**，只有合作过的中介知道。**这是本项目相对于蝉妈妈、飞瓜数据等工具的护城河。**

**数据来源**：历史聊天记录、商单复盘笔记、内部沟通记录。

**处理流程（两阶段，关键设计）**：

```
阶段一：【抽取】（入库时）
原始素材（聊天记录/复盘笔记）
   │
   ▼
[LLM 结构化抽取] → 条目 + confidence + source_quote
   │
   ▼
[人工确认] ← 【必须环节】低置信度条目高亮复核
   │
   ▼
写入 party_traits（verified = TRUE）

阶段二：【展示】（查询时）
匹配到达人
   │
   ▼
直接列出 verified=TRUE 的条目原文
   ❌ 不做二次生成/总结
```

**「只抽取、不生成」原则（重要决策）**：

| 阶段 | 做 | 不做 | 理由 |
|---|---|---|---|
| 入库时 | 抽取结构化条目 | —— | 必须有原始素材才能抽 |
| 查询时 | 列出条目原文 | **不现场生成"经验总结"** | **生成 = 引入幻觉风险 = 不可溯源** |

**为什么查询时不生成**：如果原始材料只说"达人对改稿有意见"，LLM 可能生成"该达人非常在意修改次数，建议提前沟通"——**听起来合理，但是过度推断**。一旦这个推断是错的，错误会被放大且无法追溯。**直接列事实条目，零幻觉风险。**

**字段设计**：

| 字段 | 作用 |
|---|---|
| `trait_content` | 具体内容："改稿超过2次会明显不耐烦" |
| `source_quote` | **原始材料原话引用**（防幻觉关键依据） |
| `severity` | `info` / `warning` / `critical` 三级 |
| `confidence` | 抽取置信度 0-1，低置信度提示人工复核 |
| `verified` | **仅 `TRUE` 的条目参与检索**（防脏数据污染） |

**trait 分类**：沟通偏好 / 改稿态度 / 排期习惯 / 付款要求 / 内容尺度 / 其他

**价值点**：新人接手老客户时，系统自动提示"这个达人必须提前一周给 brief，否则会取消"——**历史经验从员工脑子里，变成了可检索的公司资产。**

#### 功能 F：报价模型（双模式）

**约定（2026-09-21 确认）**：

| 模式 | 计费方式 | 字段 | 说明 |
|---|---|---|---|
| **植入（placement）** | **按时长固定档位** | `quote_embed_15s` / `30s` / `60s` | 三个固定档位 |
| **定制（custom）** | 达人自主报价 | `quote_custom` | 整条视频 |

**关键约定**：
- 两种模式**均可做**，不是二选一（`coop_models` 数组标记）
- **不涉及直播相关字段**（明确排除）
- 商单表用 `coop_mode` + `embed_duration_sec` 记录实际合作模式

---

## 5. 技术选型与论证

### 5.1 模型选型与部署方案

**部署约束**（实际环境）：
- 本地开发机显存仅 **4GB**
- 曾尝试魔搭 / 阿里云 PAI-DSW 部署，**因无法提供免鉴权公网服务而放弃**（见 5.1.5）
- 最终方案：**分层部署** —— 小模型本地跑，大模型调云端 API

#### 5.1.1 最终方案（分层部署）

| 组件 | 部署方式 | 模型 | 理由 |
|---|---|---|---|
| **Embedding** | 本地 Ollama | bge-m3（1.2GB）或 Qwen3-Embedding-0.6B（639MB） | 进程内调用，无网络往返；4GB 显存可跑；决定向量库，必须本地且稳定 |
| **主模型**（意图识别/需求解析/理由生成） | 云端 API | DashScope DeepSeek-V4-Flash | 本地跑不动可用的 Agent 主模型；API 调用零部署成本 |
| **Rerank** | V1 不启用 | —— | 4GB 显存下与 embedding 争抢资源；作为 V2 对比实验 |

**为什么这样分层，而不是全本地或全云端**：

| 方案 | 问题 |
|---|---|
| 全本地 | 4GB 显存跑不了可用的 Agent 主模型 |
| 全云端 | Embedding 若走云端 API，**换模型会导致向量库全部重建**（维度不同）；且每次检索都要网络往返 |
| **分层（选定）** | Embedding 本地 = 向量库稳定；主模型云端 = 质量有保障；**切换主模型不影响向量库** |

**关键判断**：**Embedding 是最不该频繁更换的一层**。因为它的输出被持久化到向量库，一旦更换（维度变化），所有向量必须重新生成。所以 embedding 必须一次定死并本地化。

#### 5.1.2 Embedding 模型选型论证

| 模型 | 体积 | 维度 | 上下文 | 中文效果 | 已知问题 |
|---|---|---|---|---|---|
| **bge-m3** | 1.2GB | 1024 | 8K | 优秀（长文本检索 89.7%） | ⚠️ 有报告称 GPU 模式下处理 JSON / Markdown 会输出 NaN 导致 500 报错 |
| **Qwen3-Embedding-0.6B** | 639MB | 1024 | 32K | 优秀（MTEB 64.33） | 无 NaN 崩溃问题；支持 MRL 动态降维 |
| Qwen3-Embedding-4B | 4.7GB | 2560 | 32K | 顶级（MTEB 69.45） | 4GB 显存吃紧 |

**决策：优先用 bge-m3（已下载，零成本），实测若触发 NaN 则切换 Qwen3-Embedding-0.6B。**

**为什么允许先用 bge-m3**：Ollama 的封装层与直接使用 transformers 的行为可能不同，NaN bug 未必触发。**在真实数据上实测一次，比换模型更划算。** 本项目数据规模小（20 条达人），更换成本极低，所以采用"先实测后决定"策略。

**关于 bge-m3 的已知风险与应对**：
- 若在含 Markdown 表格 / JSON 的文本上输出 NaN → 立即切换 Qwen3-Embedding-0.6B
- 切换代价：向量库重建（20 条数据，秒级）

#### 5.1.3 模型客户端抽象（架构弹性）

所有模型调用统一封装为 OpenAI 兼容协议，通过配置切换：

```python
# 配置层（.env）
LLM_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
LLM_API_KEY=sk-xxx
LLM_MODEL=deepseek-v4-flash

OLLAMA_BASE_URL=http://localhost:11434
EMBED_MODEL=bge-m3
```

**收益**：
- 主模型从 DashScope 切到魔搭/自建，**只改 3 行配置**，不动业务代码
- Embedding 从 bge-m3 切到 Qwen3-Embedding，**业务代码零改动**，但需重建向量库（已文档化为标准操作）
- 这个设计本身是简历上的加分项：**证明了对"模型可替换性"的架构思考**

#### 5.1.4 未来扩展路径

若后续获得 GPU 资源（如魔搭创空间 xGPU 审核通过），可平滑升级：

| 组件 | 升级目标 | 前置条件 |
|---|---|---|
| Embedding | Qwen3-Embedding-4B | 需重建向量库 |
| 主模型 | Qwen3-14B/32B（vLLM 服务化） | 需稳定公网服务 |
| Rerank | Qwen3-Reranker-4B | 需额外显存 |

**创空间部署的关键约束（已验证）**：
- 端口固定 **7860**，不可修改
- 只暴露**一个端口**，多服务需容器内用 **Nginx 反向代理**
- 默认文件系统**不持久**，重启即丢；持久化需用 `/mnt/workspace`
- Docker 部署需**实名认证 + 绑定阿里云账号**
- **GPU 资源需申请加入「xGPU乐园」组织**（审核制）

#### 5.1.5 部署踩坑记录（重要，避免重复尝试）

**踩坑 1：阿里云 PAI-DSW 无法为代码提供免鉴权访问**

- 现象：DSW 实例端口可映射出公网地址（形如 `https://xxx-proxy-8000.dsw-gateway-cn-hangzhou.data.aliyun.com`）
- **浏览器能访问，但代码访问被 302 重定向到阿里云登录页**
- 原因：地址带 `?appId=MAAS`，默认要求阿里云身份认证（浏览器带 Cookie，代码没有）
- 结论：**DSW 适合开发调试，不适合作为代码可调用的模型服务**

**踩坑 2：魔搭社区 Notebook 的对外访问能力有限**

- 需要「端口映射」功能手动开启，且公网端口随机分配
- 实例会话结束后服务终止，临时地址失效
- 结论：**不适合作为常驻服务**

**踩坑 3：魔搭创空间（Studio）才是正确的托管产品，但有门槛**

- 创空间可提供**免鉴权公网 HTTPS 地址**（已被社区案例验证：可匿名接收 POST 回调）
- 但 GPU 资源需申请「xGPU乐园」，免费版仅 CPU（16G 内存），跑不了有效的 embedding 推理
- 结论：**作为 V2/部署阶段的备选，不阻塞 V1 开发**

**核心教训**：**环境部署不该阻塞业务逻辑开发。** 正确顺序是：先在本地把检索链路跑通（embedding 本地跑 + 主模型调 API），部署优化放到最后。

### 5.2 编排框架选型

| 框架 | 状态管理 | 优势 | 劣势 | 本项目 |
|---|---|---|---|---|
| **LangGraph** | 图状态机 + Checkpointer | 显式控制、支持循环、支持人工中断与恢复 | 样板代码多，学习曲线陡 | **✅ 选定** |
| CrewAI | 消息/角色记忆 | 原型快，几行代码出多 Agent | 对单步执行控制弱，调试难 | 否 |
| AutoGen | 对话式 | 研究型任务强 | 对话式心智模型不适合强流程 | 否 |
| Dify 内置编排 | 可视化流程图 | 上手快 | 多 Agent 协同、条件路由、状态持久化支持不足 | 仅做外壳 |

**决策：LangGraph 做 Agent 编排，Dify 做外壳。**

**理由**：本项目的核心是**流程确定性 + 人工中断 + 状态持久化**（商单流程不能"自由发挥"）。LangGraph 的 StateGraph + Checkpointer 是这三点的工业标准实现。

**选型经验法则（写入文档以体现判断力）**：*如果能画出流程图，就用 Supervisor/StateGraph 模式；如果画不出来，说明流程本身还没想清楚，此时不该自动化，应该先上人工。*

### 5.3 存储选型

| 数据 | 存储 | 理由 |
|---|---|---|
| 达人/商单/合作记录/沟通记录（结构化） | **PostgreSQL** | 项目既有技术栈；支持 JSONB 存灵活字段；支持复杂 WHERE 过滤（混合检索的硬过滤层） |
| 文本块的向量 | **ChromaDB**（V1）→ Milvus（V2） | V1 数据量小，ChromaDB 够用且轻量；V2 若需大规模检索再迁移 |
| Agent 状态与检查点 | **PostgreSQL**（LangGraph Checkpointer） | 复用同一数据库，减少运维组件 |
| 原始文件（Excel/截图） | 对象存储 / 本地文件系统 | V1 本地即可 |

**注意**：本项目关系数据库使用 **PostgreSQL**，与既有项目技术栈保持一致。

#### 5.3.1 数据模型（已实现）

**5 张表 + 1 个视图**（对应 `src/db/schema.sql`）：

| 表 | 说明 | 关键字段 |
|---|---|---|
| `brands` | 甲方/品牌方 | 行业、公司规模、`cooperation_notes`（进向量库） |
| `creators` | 达人档案 | **量化过滤字段** + **向量检索文本字段** + 双模式报价 |
| `deals` | 商单记录 | 商务条件（修改次数/独家期/授权范围）+ `negotiation_notes`（私有资产） |
| `deal_reviews` | 商单复盘 | 复盘内容、评分 |
| **`party_traits`** | **【核心私有资产】脾气与注意事项** | `source_quote`、`severity`、`confidence`、`verified` |

**字段设计核心：两类字段分离**

| 类型 | 用途 | 例子 |
|---|---|---|
| **量化字段** | SQL 硬过滤 | `followers`、`female_ratio`、`quote_embed_30s` |
| **文本字段** | 向量语义检索 | `profile_text`、`content_style`、`cooperation_history` |

**三个承载「公司私有资产」的字段（区别于公开数据工具）**：
1. `creators.cooperation_history` —— 历史合作情况总结
2. `deals.negotiation_notes` —— 谈判过程记录
3. `party_traits.*` —— 达人/甲方的脾气与注意事项

**索引设计**：对 `platform`、`followers`、`category`、`female_ratio`、报价字段建索引，保证混合检索的 SQL 过滤层性能。

**视图 `v_creator_overview`**：聚合达人历史商单数、成交均价、已确认注意事项数——直接用作为**匹配理由的数据来源**。

---

## 6. 数据合规设计

**本节是项目的一部分，不是附录。** 在有平台方和监管的行业做数据产品，合规判断力比技术能力更稀缺。

### 6.3 分阶段落地策略（合规驱动）

```
V1：AI 生成话术 → 人工复制粘贴到微信/企微发送
    （零风险，验证话术质量）

V2：企微官方 API 接入 → 消息回调 → Agent 自动应答 + 欢迎语
    （合法，可自动化，真实产品形态）

V3：（可选）会话存档 → 聊天记录自动入知识库
    （需客户/员工授权）
```

**V1 不是妥协，是必经步骤。** 必须先证明"AI 话术有用"，客户才愿意把企微账号接进来。

### 6.4 数据合规要点清单

- [ ] 个人信息处理遵循《个人信息保护法》，需明示同意
- [ ] 群发消息需符合频次与授权要求
- [ ] 知识库中的客户/达人信息脱敏（V1 演示数据全部虚构）
- [ ] 会话存档需员工授权
- [ ] 私有化部署，数据不出域（本项目核心卖点之一）

---

## 7. 分阶段路线图

### V1：可演示版本（核心目标）

**目标**：跑通**一条**完整链路（混合检索 + 可溯源理由），能演示、能讲清楚。

**范围原则**：只做能证明项目价值的那一条链路。刊例生成、话术构思等不涉及检索的模板化功能，不构成技术壁垒，V1 全部砍掉。

| 模块 | 内容 |
|---|---|
| 数据层 | 种子数据 8 甲方 / 20 达人 / 17 商单 / 30 条 party_traits（虚构） |
| 存储 | PostgreSQL 18（5 表 + 1 视图）+ ChromaDB（本地） |
| Embedding | 本地 Ollama + bge-m3（实测 NaN 则切 Qwen3-Embedding-0.6B） |
| 主模型 | DashScope DeepSeek-V4-Flash（意图识别、需求解析、理由生成） |
| 检索 | **混合检索**：LLM 需求解析 → SQL 硬过滤 + 向量语义检索 → 合并 |
| 输出 | 可溯源理由（每条绑定数据源字段/记录 ID）+ 已验证 trait 列表 |
| Rerank | ❌ V1 不启用（4GB 显存限制），作为 V2 对比实验 |
| 人工确认 | 入库、报价、发送三类动作强制确认 |
| 界面 | 先命令行验证效果，再接 Dify |

**验收标准**：
- 输入一段甲方需求白话 → 输出带可溯源理由的达人推介单
- 每条推荐理由可追溯回原始数据字段
- 边界样本被正确纳入/排除（如女性占比 0.690 不达标、0.710 达标）
- 已验证的 critical 级 trait 被优先展示

### V2：自动化增强

- 启用 Rerank，做「有/无重排」对比实验（产出量化数据）
- 聊天记录自动结构化入档（LLM 抽取 party_traits，含人工确认）
- 多 Agent 协同：一个需求进来，自动跑完解析 → 匹配 → 对齐 → 出报价单
- 企业微信 API 接入
- 检索效果评估体系（构建评测集）

### V3：合规化与规模化

- 对接官方开放平台
- 企微全链路打通（含会话存档）
- 模型服务化（魔搭创空间 xGPU / 自建 GPU 服务器）
- 多用户权限与协作

---

## 8. 可量化成果（简历必填项）

**说明**：以下指标需在 V1 完成后实测填充。**没有数据的项目在简历上会被追问到底。** 建议在开发过程中同步构建评测集。

| 维度 | 指标 | 测量方式 | 目标值（待实测填充） |
|---|---|---|---|
| **检索质量** | Recall@5 | 构建 30-50 条标注问题集 | ___ |
| | 纯向量 vs 混合检索准确率对比 | A/B 对比（**核心实验**） | ___ |
| | 引入 Rerank 后的 MRR 提升 | 对比实验（V2） | ___ |
| | 边界样本判定准确率 | 0.690 vs 0.710 类样本 | ___ |
| **效率提升** | 单次达人匹配耗时 | 人工 vs 系统 | 人工约 ___ 分钟 → 系统约 ___ 秒 |
| **抽取质量** | party_traits 抽取准确率 | 人工抽检 | ___ |
| **系统性能** | 端到端响应延迟 | 压测 | ___ |
| | 单次查询 token 消耗 | API 统计 | ___ |

**简历表述参考**：
> "构建混合检索链路（LLM 需求解析 → PostgreSQL 结构化过滤 + 向量语义检索），解决纯向量检索无法处理数值范围条件的问题；在自建评测集上 Recall@5 达 __%，较纯向量检索提升 __ 个百分点。"

---

## 9. 简历表达建议

### 9.1 项目描述模板

> **广告中介商单 Agent 工作台**（个人项目）
> **技术栈**：Python + PostgreSQL + ChromaDB + Ollama(bge-m3) + DashScope DeepSeek + Dify
>
> - 针对广告中介行业"历史商单经验无法复用"的痛点，设计并实现私有化 AI 工作台，覆盖达人商单匹配、建联对齐全流程。
> - 设计**混合检索链路**：LLM 需求解析 → PostgreSQL 结构化过滤 + 向量语义检索，解决纯向量检索无法处理数值范围条件（粉丝量、报价区间）的问题。
> - 实现**可溯源匹配理由生成**，每条推荐结果绑定原始数据字段与记录 ID，杜绝无来源的伪精度评分。
> - 设计 **party_traits 私有经验资产模型**：从聊天记录/复盘材料抽取达人脾气与注意事项，保留 `source_quote` 原文引用防幻觉，仅人工确认后的条目参与检索。
> - 建立**模型可替换的客户端抽象层**（统一 OpenAI 兼容协议），主模型与 embedding 均可通过配置切换而不改动业务代码。
> - 完成部署踩坑与方案收敛：验证 PAI-DSW / 魔搭 Notebook 均无法提供免鉴权公网服务，最终采用「Embedding 本地 + 主模型云端」分层方案。

### 9.2 预期追问与准备

| 面试官可能问 | 准备要点 |
|---|---|
| 为什么不用纯向量检索？ | 数值范围过滤是语义检索的盲区，举具体例子（粉丝量 5-30万、报价区间） |
| 为什么 Embedding 本地、主模型上云？ | ① 4GB 显存跑不了可用主模型；② **embedding 换模型会导致向量库全量重建**，必须本地定死；③ 主模型可随时切换不影响向量库 |
| bge-m3 你不是说有 bug 吗？ | 说明是"先实测后决定"的策略，并讲清 Ollama 封装层可能规避该问题，以及切换成本极低 |
| 匹配度怎么算的？ | **不要给伪精度**。强调"每条理由绑定数据源"的设计 |
| trait 抽取会不会有幻觉？ | 讲 `source_quote` 原文引用 + 人工确认 + 查询时不生成只列事实 三重防线 |
| 数据哪来的？合规吗？ | 主动讲合规决策：不做爬虫、走企微官方 API。**这是加分项** |
| 效果如何衡量？ | 讲评测集构建方法 + 纯向量 vs 混合检索的对比实验 |
| 为什么不用 Dify 内置知识库？ | Dify 内置是纯向量检索，**做不了混合检索与可溯源输出**，所以只做外壳 |
| 为什么不部署到魔搭？ | 讲三个踩坑（见 5.1.5），体现"验证后收敛"的判断力，而不是"部署失败" |
| 为什么知识图谱不做了？ | 见第 10 章 |

---

## 10. 明确不做的事（附理由）

**明确列出"不做"，是设计成熟的标志。**

| 不做 | 理由 |
|---|---|
| **平台爬虫** | 法律、工程、商业三重风险，见 6.1 |
| **个人微信协议逆向** | 封号风险不可控，见 6.2 |
| **知识图谱（V1）** | 见下方详述 |
| **全自动对外发送** | 商业风险，必须人工确认 |
| **达人公开数据全量同步** | 已被平台与数据商做透，无差异化空间 |

### 10.1 知识图谱为何不做（V1）

**KG 的唯一优势区是多跳关系推理**，例如"达人 A 合作过的甲方，还投过哪些达人"。

**但本项目四类真实查询中，KG 仅占一类**：

| 查询类型 | 例子 | 适用技术 |
|---|---|---|
| 属性过滤 | 粉丝 5-30 万的美妆达人 | 结构化查询 |
| 语义相似 | 这个需求像不像以前那单 | 向量检索 |
| 精确关系 | 达人 A 和甲方 B 合作过吗 | 关系表 join |
| **多跳推理** | 达人 A 的甲方还投过谁 | **KG 独占** |

前三类覆盖 85% 的真实需求，已被"混合检索"覆盖。多跳查询属锦上添花。

**KG 的真实成本**：
1. **实体与关系抽取本身就是 NLP 任务**，准确率通常 70-85%，意味着图谱中存在错误边，会导致多跳推理输出错误结论，且**极难排查**；
2. 数据更新需同步图谱，引入 Neo4j 使运维复杂度翻倍；
3. V1 数据量（几十条）根本不足以体现 KG 价值。

**结论**：V1 不做。若后续要做，**前置条件是**：先用 LLM 从聊天记录抽取三元组，人工抽检准确率需达 85%+，达标才上图。**抽不准就不上，上了是负资产。**

---

## 11. 当前进度与待办

### 11.1 已完成（截至 2026-09-21）

| 项 | 状态 |
|---|---|
| PRD 与技术方案 | ✅ 完成 |
| 数据库 Schema（5 表 + 1 视图） | ✅ 完成并执行 |
| 种子数据（8 甲方 / 20 达人 / 17 商单 / 30 trait） | ✅ 已导入 PostgreSQL 18 |
| 数据校验脚本 | ✅ 边界样本验证通过 |
| 项目骨架与配置 | ✅ 完成 |

### 11.2 待办

| 编号 | 事项 | 影响 | 状态 |
|---|---|---|---|
| 1 | Ollama embedding 实测（验证 bge-m3 是否触发 NaN） | 决定最终 embedding 模型 | **下一步** |
| 2 | 向量化入 ChromaDB | RAG 基础 | 待办 |
| 3 | 需求解析（Prompt 设计与验证） | 混合检索的前置 | 待办 |
| 4 | 混合检索实现与调优 | **核心技术** | 待办 |
| 5 | 可溯源理由生成 | **核心差异化** | 待办 |
| 6 | 评测集构建（30-50 条标注问题） | 可量化成果 | 待办 |
| 7 | Dify 接入 | 演示形态 | 最后 |
| 8 | DashScope API Key 配置 | 主模型可用 | 待办 |

---

## 附录 A：术语表

| 术语 | 说明 |
|---|---|
| **星图服务商** | 经巨量星图平台认证，为客户与平台间提供达人筛选、建联、履约等服务的企业 |
| **刊例** | 达人商业合作的标准化报价与交付说明文档 |
| **建联** | 商务建立联系，即中介与达人/甲方的首次商务沟通 |
| **对齐** | 商单达成后，双方就交付细节（坑位费、排期、修改次数等）达成一致的过程 |
| **植入（placement）** | 在达人原有内容中植入品牌信息，按时长固定档位（15s/30s/60s）计费 |
| **定制（custom）** | 达人专门为品牌制作的整条视频，达人自主报价 |
| **混合检索** | 结构化过滤 + 向量语义检索的联合查询方式 |
| **party_traits** | 达人/甲方的脾气、癖好、注意事项，公司核心私有经验资产 |
| **Human-in-the-loop** | 在自动化流程中插入人工确认环节的机制 |
| **可溯源理由** | 每条推荐理由绑定具体数据字段或记录 ID，禁止无来源的评分数字 |

## 附录 B：参考系统

| 系统 | 类型 | 参考价值 |
|---|---|---|
| 巨量星图 | 平台方 | 业务流程、角色定义 |
| 小红书花火 | 平台方 | 跨平台对标 |
| 蝉妈妈 / 蝉圈圈 | 数据商 | AI 商务助手产品形态（已被验证的方向） |
| 飞瓜数据 / 千瓜数据 | 数据商 | 达人库字段设计 |
| Dify / RAGFlow / FastGPT | 开源平台 | 工作台底座 |
| LangGraph / CrewAI / AutoGen | 开源框架 | Agent 编排 |
| CreatorIQ / Traackr / HypeAuditor | 海外产品 | 功能天花板 |

---

*文档结束*
